<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="FeedCustomizer.Dialogs.StartupFailureDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource DefaultContentDialogStyle}"
    x:Uid="StartupFailureDialog"
    DefaultButton="Close">
    <Grid MinWidth="300" MaxWidth="460" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0"
                      MaxHeight="420"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="ErrorTextBox"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     BorderThickness="0"
                     Padding="0"
                     FontFamily="Consolas" />
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Visibility="Collapsed" x:Name="FeedbackStatusPanel">
            <ProgressRing Width="16" Height="16" IsActive="False" x:Name="FeedbackProgressRing" />
            <TextBlock x:Name="FeedbackStatusText" TextWrapping="Wrap" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</ContentDialog>
