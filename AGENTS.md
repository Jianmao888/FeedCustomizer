# FeedCustomizer 开发与重构规范

本文件是本仓库中 AI 与开发者进行代码修改时必须遵循的工作规范。目标是让项目在持续迭代中保持职责清晰、可诊断、可恢复，并避免以临时补丁叠加复杂度。

## 基本原则

- 先理解现有实现、调用链和数据所有权，再修改代码；不要只针对表面症状追加特殊分支。
- 修改应尽量小且完整：一个变更应包含实现、必要的测试或验证，以及与行为变化相符的文档/本地化内容。
- 优先复用已经存在且职责正确的逻辑。复用前确认其线程模型、错误语义、数据所有权和副作用符合新场景；不要为了复用而扩大原类职责。
- 不复制粘贴相似流程。出现第二处相同的编排、文件操作、PowerShell 调用或错误转换时，应优先提取明确的服务或帮助方法。
- 不以吞掉异常、无限重试、固定延迟或“清空后重来”作为常规控制流。临时兼容措施必须注明触发条件、退出条件和后续移除条件。
- 每次较大的改动完成并通过必要验证后，应自动执行 Git 提交；提交信息必须根据实际变更自动生成，准确概括改动内容和目的，避免使用含糊或与变更不符的描述。较小的修改无需强制单独提交。
- 不修改与当前任务无关的文件、格式或用户已有未提交改动。

## 命名、格式与注释

- 遵循仓库现有 `.editorconfig` 与 C# 常规命名：类型/公开成员使用 PascalCase，私有字段使用 `_camelCase`，异步方法以 `Async` 结尾。
- 代码、标识符和日志可使用英文；面向用户的文本必须走资源文件，不在业务代码中新增硬编码 UI 文案。
- 注释、XML 文档注释、提交说明和新增开发文档使用中文。
- 注释解释“为什么这样做”、约束、并发关系、失败恢复和 Windows 平台行为；不要复述代码字面含义。
- 对公开类型、公开方法、跨层接口、复杂状态转换和非显然的平台限制写中文 XML 文档注释。
- 方法内部也应根据复杂度在关键阶段、分支和异常恢复处添加适当的中文注释，使开发者能够仅根据注释理解方法的执行逻辑、状态变化和控制流；简单且自解释的方法无需逐行添加注释。
- 过时的注释必须与代码一并更新或删除。不要保留与当前实现相矛盾的历史说明。
- 避免为显而易见的每一行添加注释；优先用准确命名、短方法和类型表达意图。

## 分层与 MVVM

- UI 层（`Pages`、`MainWindow`、XAML code-behind）只处理控件生命周期、导航、动画、窗口/Dispatcher 适配和显示对话框，不承载业务决策、文件读写、PowerShell 或注册流程。
- ViewModel 负责可绑定状态、命令、输入校验和将 UI 意图交给应用服务；不得直接拼接 PowerShell 命令、遍历/删除文件、操作注册表或编排 Provider 部署细节。
- ViewModel 不直接依赖具体控件、`Window` 或 `Page`。如需 UI 动作，使用已有事件/抽象，或新增语义明确的 UI 请求接口。
- Core 中按职责拆分：模型/纯规则、应用服务（用例编排）、基础设施适配器（文件、进程、PowerShell、注册表、AppX）和持久化实现。不要把新业务继续堆入泛化的 `Tools` 类。
- 不新增全局可变静态状态作为跨页面通信手段。确需进程级状态时，明确其生命周期、并发规则和重置策略，并优先通过服务接口暴露。
- 每个公开操作应有清晰的输入、输出、失败语义和副作用；布尔返回值不足以表达失败原因时，使用包含状态、诊断和可恢复性的信息对象或结果类型。

## Provider、资源与文件

- 将 Provider 生命周期视为一个受串行保护的领域流程：检查、准备、发布、注册、卸载、修复必须由同一个部署协调服务编排。
- Provider 的模板资源、用户可编辑工作副本、实际注册部署副本必须有明确且独立的目录职责。用户订阅源和下载图片不得与可整体替换的部署产物混放。
- 用户数据的唯一真相只能有一份；注册目录是可再生部署产物，不能反向作为用户配置来源。
- 发布文件时优先采用“临时目录构建/校验 → 原子替换”的策略。不得先递归清空唯一有效副本再尝试复制。
- 所有写入、删除、替换操作都必须限定到已验证的应用专用目录。不得使用宽泛通配符或由未验证输入构建递归删除路径。
- 文件锁、进程占用、异常退出是预期场景：重试需设置有限次数、退避/间隔、最终错误和可恢复路径。不要用无限轮询。
- 清单模板、运行时生成清单和用户配置要在名称与类型上区分，避免同名文件承担多种含义。

## PowerShell、进程与提权

- PowerShell 仅作为基础设施边界，不作为业务逻辑承载位置。业务层调用语义化接口，例如“注册 Provider”“查询注册状态”。
- 统一通过执行器运行普通与提权 PowerShell；执行器负责参数传递、超时、取消、标准输出/错误输出、退出码和诊断信息。
- 优先使用 `ProcessStartInfo.ArgumentList`、脚本文件或受控参数传递；避免将路径、用户输入或复杂逻辑拼接进 `-Command` 字符串。
- 每次 PowerShell 操作必须设定成功判定规则、超时与错误分类。不要仅依赖本地化错误文本；可识别的错误码应转换为稳定的领域错误。
- 提权必须仅覆盖确实需要管理员权限的最小操作，并明确处理用户取消 UAC。临时脚本/日志在 `finally` 中做最佳努力清理，清理失败不得掩盖原始错误。
- 终止 Provider 进程前必须验证其可执行路径属于本应用部署目录，不能仅按进程名操作。

## 启动、并发与 UI 状态

- 启动流程应由单一启动协调器管理，并产生明确的阶段结果。Splash/遮罩只能反映状态，不能承担同步或业务正确性。
- 任一启动失败路径都必须保证结束 Splash 或进入可操作的失败界面，避免 `TaskCompletionSource` 未完成造成永久等待。
- 耗时任务必须支持取消或至少有有限超时；关闭窗口、重复点击和页面离开时不得留下不可观测的关键任务。
- 对同一 Provider 的注册、卸载、发布只允许一个操作同时运行。锁应位于协调服务内部，避免 ViewModel、页面与基础设施层各自加锁。
- UI 状态更新必须回到 UI 线程；后台任务不得直接修改绑定集合。
- 避免 `async void`，仅事件处理器可以使用；事件处理器必须捕获并转交异常，不能让关键异常静默丢失。

## 错误处理与可观测性

- 基础设施层返回结构化错误和诊断，不直接弹 UI 对话框。由 ViewModel 或启动协调器决定显示、重试、降级或引导修复。
- 不要使用空 `catch` 或只写 `Debug.WriteLine` 后伪装成功。非致命失败要明确降级；关键失败要向上返回。
- 日志应包含操作名称、目标路径/包标识、阶段、退出码和可安全公开的诊断。不得记录用户敏感数据、完整不可信输入或密钥。
- “修复”操作必须幂等：重复执行不应破坏已有正确状态。

## 构建、依赖与测试

- 构建产物必须写入 `obj`、`bin` 或明确的中间目录，不得在构建前删除或重写受版本控制的 `Resources` 源目录。
- 子项目发布与主项目打包应通过显式 MSBuild 依赖和中间产物连接，不能依赖 IDE 手工构建顺序。
- 新增依赖前先检查 .NET/Windows App SDK 现有能力；依赖应有明确用途，避免为单一小功能引入大型库。
- 修改清单解析/生成、路径处理、部署计划、状态转换和错误分类时，优先补充不依赖 UI 的单元测试。
- 对注册、文件替换和 PowerShell 等平台副作用，使用接口隔离并为核心编排编写替身测试；真实集成验证应有清晰前置条件和清理步骤。

## 修改前后的检查清单

修改前：确认数据源、调用方、并发入口、失败路径和现有测试；检查工作树，保留用户改动。

修改后：确认 MVVM 边界未被打破；新增注释为中文且解释原因；异常没有被静默吞掉；目录和 PowerShell 参数经过约束；构建与相关测试/验证通过；如行为变化，更新资源与文档。
